# FEDS / Adaptive Sparsification — install first (e.g. in Colab: !pip install -r requirements.txt)
# Core: server + MNIST/CIFAR/Speech clients
flwr>=1.5.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
numpy>=1.21.0
tensorboard>=2.10.0

# Plotting and validation
matplotlib>=3.5.0
scipy>=1.9.0

# Optional: CKA-based layer analysis (FindingK, FindingKSpeech)
# torch_cka>=2.0
